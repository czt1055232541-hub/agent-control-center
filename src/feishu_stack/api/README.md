# api

这个目录是后端 HTTP 接口入口。前端网页和脚本通常通过这里访问后端功能。

文件说明：

- `app.py`：FastAPI 主应用。定义 API 路由、请求参数、返回结构、静态页面入口。
- `security.py`：接口安全相关逻辑，比如本地控制 token 的读取、生成和校验。
- `__init__.py`：包初始化文件，本身没有业务重点。

学习建议：

- 想知道某个网页按钮最终调用了哪个接口，先搜 `app.py`。
- 想改接口鉴权、token 校验、接口保护方式，看 `security.py`。
