# database

这个目录是数据库相关内容的预留位置。

当前项目还没有正式数据库，所以这里暂时只是学习和扩展用占位目录。

未来建议放这些内容：

- `schema/`：表结构或数据结构定义。
- `migrations/`：迁移脚本。
- `seeds/`：初始化数据。
- `local/`：只在本机使用的数据文件，这类内容要加入 `.gitignore`。

现在你可以把这里理解成“以后如果项目开始持久化存数据，就从这里长出来”。
