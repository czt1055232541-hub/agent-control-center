# Known-good direct dependency set for Python 3.13 on Windows.
# This is intentionally not a pip freeze: transitive resolution remains the
# installer's responsibility within the compatible ranges in pyproject.toml.
# Refresh only after the full test suite and frontend build pass.
fastapi==0.137.2
prometheus-client==0.25.0
psutil==7.2.2
pydantic==2.13.4
PyYAML==6.0.2
starlette==1.3.1
uvicorn==0.49.0

# Development and test tools
httpx==0.28.1
pytest==9.1.0
