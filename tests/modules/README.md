# tests/modules

这里按功能域测试后端业务模块。

子目录说明：

- `agent_array/`：Agent 阵列测试。
- `backup_migration/`：备份与迁移测试。
- `logs_diagnostics/`：日志与诊断测试。
- `model_provider/`：模型与 Provider 测试。
- `operations/`：操作编排测试。

学习建议：

- 想知道某个功能域应该如何工作，直接看同名测试目录最直观。
