# web/src

这是前端源码主目录。

子目录说明：

- `app/`：前端应用外壳，比如主入口、整体布局、全局样式。
- `api/`：前端调用后端 API 的小工具。
- `components/`：跨页面复用的通用组件。
- `hooks/`：React 自定义 Hook，负责请求数据和组织前端状态。
- `modules/`：按页面功能域拆分的前端代码，是最推荐你阅读的前端目录。
- `types/`：前端通用类型定义。

学习建议：

- 想知道页面从哪里开始渲染，看 `app/main.tsx`。
- 想改某个具体功能区块，优先去 `modules/`。
