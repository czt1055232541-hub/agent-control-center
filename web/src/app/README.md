# app

这个目录放前端应用的外壳层代码。

文件说明：

- `main.tsx`：前端主入口，也是页面总装配点。大部分模块最后都会在这里拼起来。
- `Sidebar.tsx`：左侧导航栏，定义页面功能域入口。
- `styles.css`：全局样式。

学习建议：

- 想调整页面整体布局、入口顺序、默认显示内容，看 `main.tsx`。
- 想改左侧菜单，看 `Sidebar.tsx`。
- 想改全局配色或整体外观，看 `styles.css`。
