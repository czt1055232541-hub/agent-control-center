# 恢复与回滚

1. 保存当前单仓的 `git status --short --branch`，不要清理未知修改。
2. 若配置写入失败，保持当前有效文件；需要回退时选择最新的 `*.bak-*`，先校验再原子替换。
3. 代码阶段按独立提交回滚，不使用会丢失工作区改动的硬重置。
4. 重启只针对已确认需要加载新代码的组件，并再次检查健康接口、PID、端口和日志。
5. 物理合并切换失败时，停止新路径进程、恢复配置备份，并使用预合并 bundle 或原飞书仓库恢复；不要形成新旧路径混合运行状态。
