# 兼容入口治理

当前识别到 26 个 `src/feishu_stack` 顶层兼容模块，包括 app、config、models、process、operations、status、provider、日志、备份及组件控制入口。它们统一转发到 `api/`、`core/` 或 `modules/` 下的新实现。

分类：

- 仍被使用：`app.py`、`config.py`、`models.py`、`operations.py` 等仍被测试、脚本或历史启动命令导入。
- 可迁移：已有明确新路径且仓库内调用方可逐步更新的模块。
- 未知：无法通过静态搜索证明没有外部调用方的入口，一律按仍被使用处理。

退役条件必须全部满足：仓库搜索无调用方、运行日志在约定观察期无弃用告警、旧命令回归已迁移、发布说明给出替代路径。满足前只保留带弃用提示的薄包装，不删除。
