# 实施基线（2026-07-13）

- ACC 分支：`develop`，实施前存在 API、配置中心、路由规则、飞书连接和前端入口相关未提交改动。
- 飞书 Agent 分支：`master`，实施前存在本地辅助脚本和未跟踪目录。
- 实施边界：不覆盖、删除或整理上述未知改动；不物理合并仓库。
- 端口基线：8765、5173、18789、18790、38440 在检查时均未监听。
- 进程基线：未执行终止操作；端口无监听，因此没有需要确认或停止的目标进程。
- 隐私边界：本地配置被 Git 忽略；文档和新增测试只使用占位标识。
- 初始门禁：配置契约/API 定向测试 54 项通过，ACC Web 生产构建通过。
- 最终门禁：后端 271 项测试通过；ACC Web 生产构建通过；CLI `status --json` 返回有效 JSON；`pip check` 无破损依赖；`git diff --check` 通过。
- 配置契约冒烟：状态为有效，飞书 Agent 对比配置可读取，发现 2 个共享字段漂移；未自动修改本地配置。
- 隐私扫描：新增补丁中未发现真实格式的飞书 open ID 或 chat ID。依赖审计工具 `pip-audit` 当前未安装，因此只完成依赖图一致性检查。
