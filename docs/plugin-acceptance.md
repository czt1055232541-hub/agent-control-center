# 插件化验收记录

依据 ai-project-delivery-workflow-version1-6 的分阶段验证与完成审计要求维护。

## 当前结论

整体尚未验收通过。当前 ACC 为 v0.15.0，DSH 适配器为 0.1.3；十个内建功能已有前后端注册入口，生命周期和外部发行包挂载已有自动化测试，框架设置页支持持久化启用配置。剩余验收重点是共享服务依赖隔离、当前 DSH 宿主的端到端调用与主功能回归，以及卡片和设置页的真实视觉与点击行为。

| 要求 | 当前证据 | 未完成项 |
| --- | --- | --- |
| DSH 源码不修改 | 源码仓库状态干净；适配器通过 profile tarball 安装 | 发布后再次核对源码状态 |
| DSH 能正常启动 | 0.1.1 适配器通过实际启动，3080 返回认证后的 HTML | 在线、离线、卸载及其他工具回归 |
| 卡片美观且可点击 | 客户端实现已提供 | 浏览器控制运行时不可用，尚缺真实视觉和点击证据 |
| ACC 框架与功能解耦 | 十个后端与十个前端插件入口；entry-point 发现；生命周期和启动禁用测试 | 浏览器禁用行为、共享服务依赖审计 |
| 后续功能挂载 | 真实外部发行包在隔离目录安装；新进程发现清单、API、HTML 页面；禁用后 404 | 浏览器点击验收及 DSH 对新增插件的实际调用验证 |
| 分阶段发布回退 | develop、版本标签及改造前远端备份分支 | 后续阶段持续推送 |

## 本阶段验证

- v0.15.0 插件设置与界面统一：框架固定设置入口列出所有已安装插件，禁用项可重新启用；使用原子写入与单份前版本备份，严格校验依赖、框架不可关闭，保存后重启生效，环境变量覆盖时只读。新增真实独立宿主进程测试证明全功能停用后设置仍可访问、保存重新启用后下一进程恢复功能。框架头部移除业务状态请求；本地工具明确为外部应用，保留 ID/API/用户数据。全量 Python 343 通过（3 条既有警告），Web 11 通过，类型检查和构建通过。仅重启 ACC 至 PID 33808，健康版本 0.15.0，设置接口 11 项，未授权 PUT 返回 401，线上 JS/CSS 与构建逐字节一致；DSH PID 24064 未受此次 ACC 更新影响，已安装适配器验证仍通过。线上未替用户关闭任何插件。浏览器连接仍因可信桥接不可用而失败，实际设置点击尚待用户确认。详见 plugin-settings.md。
- DSH 本地运行修复（不改源码）：修复启动器旧令牌误判后重复启动导致 EADDRINUSE；实际冷启动认证 HTML 200，再次执行 start-dsh.cmd 复用同一 PID。自定义 ptc-controlled 的 persona.text 改为 prefix，保留内容，实际创建空验收会话挂载成功、未调用模型或工具。启动器 3 项隔离测试通过，原文件本地备份，DSH 源码工作树干净。用户确认新建对话自行恢复，按要求不再扩大该间歇性问题的排查，不将预设修复声称为点击问题根因。

- 日志插件启动依赖：新增独立进程挂载测试，先复现加载日志插件时连带导入 Model Provider.codex_desktop 和 Local Tools.registry；将这两项导入延迟到对应日志请求后，隔离与 API 测试 65 通过、2 条既有警告。未改变日志 URL 或实际探测行为，未重启线上 ACC；此项只证明启动导入边界，不证明禁用功能后的请求隔离，后者仍待实现。

- 2026-09-18 用户实际实例核对：4180 监听进程使用源码 CLI 的 Web profile，DSH_HOME 为既有 dsh-runtime；该 profile 安装适配器 0.1.3。按用户要求重启后 PID 26692，用户截图确认 DSH 历史会话页面显示。截图不是 ACC 工具调用证据。随后发现 ACC 8765 无监听，在线验证首次失败；使用既有启动命令恢复 ACC（PID 464），健康接口 0.14.0、清单 11 项，已安装包验证重跑退出 0（在线、错误响应、错误结构、超时、离线及 Cordis 两轮加载/释放均通过）。DSH 源码工作树干净。已通过 ACC 原生 --open 请求打开页面，但尚无该页面截图。新增适配器 README 的真实卡片操作与人工验收步骤；不将独立调用脚本等同于当前 DSH 会话调用。

- Dashboard 组合卡片禁用约束：配置、看门狗、诊断、Agent 拓扑分别按功能归属过滤；拆开原先绑在一起的 Agent 拓扑与诊断卡片，基础设施卡片归属 Dashboard。Web 8 项测试、类型检查、构建通过。浏览器通道重试仍不可用，布局与真实点击尚未验证。
- 2026-09-18 DSH 实际页面交付验收：用户确认此前进程为手动停止，并授权需要时启动。重新启动 Web profile 成功；使用本次启动认证入口获取真实 HTML，boot graph 共 55 项，包含 dsh-plugin-acc 及 renderer/tool 依赖，ACC 属于初始 batch。认证获取的 ACC 客户端脚本与当前源码一致，包含模块工厂和 0.1.3 主题变量。令牌仅用于内存中的本地请求，未写入文档或仓库。此结果证明实际宿主交付，不证明浏览器执行、视觉及点击通过。
- v0.14.0 汇总上线：此前全量 Python 335 通过，本轮仅版本元数据变更；Web 7 项、类型检查、构建通过。仅重启核实的 ACC 服务，线上版本 0.14.0、11 项清单、指标和构建资源一致。已安装 DSH 适配器的故障降级与真实 Cordis 生命周期脚本仍通过。重要运行变化：本轮发现 3080 已无监听，无法沿用此前 DSH 保持运行的结论；首次重启脚本因此在前置检查终止、未停止进程，随后只更新 ACC。DSH 实际宿主启动和浏览器验收仍需继续。
- 2026-09-18 连续隔离迁移后的全量回归：335 项通过、3 条既有警告、退出码 0。首次运行输出句柄失效，检查确认无该轮 pytest 后使用另一隔离目录重跑；首次不计为通过。两个测试临时目录已清理，DSH 源码工作树再次确认干净。该轮未重启线上服务，近期描述/操作/备份/飞书/诊断迁移仍待汇总发布。
- Agent/诊断反向依赖修正：诊断解释与动作建议归入 logs_diagnostics.explanations，诊断 API 直接使用自身实现；Agent 聚合旧 explained_diagnostics 入口保留延迟委托。独立进程挂载 Agent 不加载日志诊断包，Agent 行为与 API 回归合计 72 通过。日志诊断仍使用 Agent 注册表生成跨功能解释，尚不能宣称所有跨功能依赖已消除；本阶段未更新运行服务。
- 飞书授权探测归属修正：Feishu router 不再通过旧 diagnostics 兼容入口加载日志诊断功能，授权状态探测实现放入 feishu_connection.auth；日志诊断函数保留委托接口。独立进程挂载飞书插件不加载 logs_diagnostics，模拟 CLI 失败保持结构化返回，飞书与诊断测试 13 通过。未调用真实授权命令或改变用户授权，尚未重启线上服务。
- 备份实现归属修正：混合启用扫描确认备份插件加载 Model Provider 的原因是清理实现放在模型配置模块。现将保留策略与清理实现归还 backup_migration，模型配置旧 clean_backups 入口兼容委托。以临时目录检查保留最新备份和目标备份，独立进程确认备份服务不导入模型插件；备份与模型配置测试 15 通过。删除原两项只检查跨模块委托的测试，替换为行为与隔离测试。未清理用户真实备份，未重启线上服务。
- 内建描述延迟加载：metadata 集中于不导入业务模块的 builtin_descriptors，原十个 create_plugin 入口兼容转发；只有挂载时导入路由实现。新进程测试先失败、迁移后通过；全部功能禁用时宿主不导入十个功能包（仍保留 operations 宿主控制设施）。全量测试 331 通过，随后新增框架宿主导入检查并单独运行两项隔离测试通过。混合启用时的跨功能服务依赖尚未消除；本阶段尚未重启线上 ACC。
- 公共操作设施迁移：实现归属 core.operations，两个 API 宿主入口解除对功能目录中执行设施的依赖；旧路径是同一模块对象，锁与记录不重复。新增测试先因 core 入口不存在失败，迁移后验证共享锁及配置加载失败释放锁；操作插件与 API 回归合计 110 通过。修复原实现配置加载失败时锁泄漏；未执行真实控制操作，本阶段尚未重启线上 ACC。
- DSH 工具生命周期验证：verify-installed.mjs 创建真实 Cordis Context、SystemPrompt 和 ToolRuntime，连续两轮加载/释放已安装 ACC 插件；acc_overview 正确注册并在 fiber.dispose 后消失，同宿主独立哨兵工具在各阶段均正常执行。清理工具与提示服务 fiber 后脚本以 0 退出。初次脚本误用不存在的 Context.dispose，修正为服务 fiber 清理并重跑成功；不将初次失败计为通过。这证明工具注册所有权，不等同于完整 DSH 会话或浏览器回归。
- 独立 profile 安装/卸载验证：在专用 acc-audit profile 中安装适配器 0.1.3，DSH dump-config 成功且包含 acc-integration；执行 remove 后，package.json 依赖与组合配置中的 ACC 条目均消失。原 Web profile 仍安装 0.1.3、DSH PID 5032 未变。新 profile 初次安装遭遇 pnpm 项目索引 symlink EPERM，使用该索引到测试 profile 的目录联接后成功；测试 profile 和索引联接保留供复验。此验证覆盖包管理和配置发现/移除，尚不覆盖卸载后浏览器或模型工具回归。
- v0.13.1 汇总发布：全量 Python 328 项通过（3 条既有警告），Web 7 项、类型检查、生产构建通过；同步修正前端 lockfile 的根包版本。仅重启 ACC 后，线上健康版本为 0.13.1、指标端点正常，DSH PID 5032 保持运行。此前统计迁移及 Dashboard 禁用状态流修复已随本次重启上线。测试临时产物已清理。此为阶段发布，非整体视觉/交互验收通过。
- 框架模式回归：隔离进程禁用全部内建功能后，健康检查 200、清单仅 acc.framework、业务 HTTP 路由 404；修复状态 WebSocket 仍开放的问题，Dashboard 禁用时以 1008 拒绝连接且不调用业务状态采集。生命周期及框架统计测试合计 6 通过。尚未重启线上服务以应用此修复；功能 Python 包仍可能因现有静态导入加载，不能将路由禁用声明为物理卸载。
- 2026-09-17 profile 依赖警告审计：pnpm peers check 报缺失/冲突，但从已安装 ACC 适配器的真实模块路径解析，Cordis=4.0.2、DSH tools/ui-renderer/ui-tool=0.1.5-rc.2、React=18.3.1，均满足当前声明。DSH profile 机制使用宿主 fallback 并关闭自动安装 peers；未为消除警告而安装重复宿主库。验证脚本加入依赖解析输出；原有 dsh-free-search 的警告未修改。解析成功不代表浏览器已成功激活客户端。
- 适配器 0.1.3 主题契约：对照 DSH design-platform.css，将不存在的颜色变量替换为实际背景、文字、边框和状态别名，按钮显式继承前景色和字体；Web 7 项测试通过。已安装到 Web profile，安装包调用验证再次通过。尚无浏览器深浅色截图，不能把变量名验证当作视觉验收；已请求用户协助提供实际卡片和点击证据。
- 框架依赖审计：统计服务迁入 core.metrics，宿主和操作执行器解除对日志诊断模块的统计依赖；原路径保留同一模块对象的兼容别名。独立进程测试确认 core 导入不加载诊断包、兼容路径不会重复注册指标；该测试与 API 回归合计 101 通过。此阶段未重启线上 ACC，运行更新留待下一阶段合并验证；其他业务服务依赖隔离仍未完成。
- 2026-09-17 已安装适配器调用验收：新增 verify-installed.mjs，直接导入 Web profile 安装的 0.1.2 包，使用真实 DSH defineTool 验证输出 schema 和文本渲染；在线 ACC 返回 11 项。独立模拟服务的 HTTP 503、无效 JSON、错误清单结构、250ms 超时和服务退出均返回 offline 而不抛异常。未调用模型、未停止真实 ACC/DSH。此证据仅证明工具调用边界，不证明浏览器和宿主其他功能回归完成。
- 2026-09-17 写接口审计：配置中心 set/delete/import/export 和路由规则 set/delete/batch 共 7 个接口补齐控制令牌校验，前端同步发送令牌。隔离模拟服务验证 401/403 时零业务调用、合法令牌正常分发；全量 Python 326 通过、Web 6 项测试、类型检查和生产构建通过。重启 ACC 后两个写入口的无令牌请求均返回 401，未执行真实配置写入。浏览器通道重试仍报可信桥接不可用，因此视觉和真实点击未验收。
- 2026-09-17 运行更新验收：全量 Python 测试 319 通过（3 条弃用/未注册标记警告），Web 生产构建通过。核实进程命令及工作目录后，仅重启旧 ACC PID 27028；新 PID 31960、端口 8765，健康接口版本 0.13.0。插件清单 11 项（含框架），线上两个 JS/CSS 资源与本机构建逐字节一致，未知 API 返回 404。DSH 原 PID 5032 保持运行。隔离测试临时目录已清理；真实浏览器与 DSH 主功能回归仍未完成。
- 适配器 0.1.2：对照当前 DSH 模块加载器源码，发现此前普通 ESM 客户端不符合经典脚本工厂契约，已修复。回归测试改为直接执行交付脚本、检查注册 ID、从工厂注入共享 React 后真实渲染，6 项通过；已生成独立 tarball。运行版本核对发现 ACC 8765 仍为 v0.5.0，需更新该服务，不能用源码版本代表当前运行版本。
- v0.13.0 后续卡片审计：修复异常 content 导致渲染失败，以及未校验跳转协议的问题；仅允许无内嵌凭据的 HTTP(S) 地址。Web 测试 6 项通过，新增真实 React 服务端渲染检查覆盖在线、离线、处理中、异常结果和危险地址。注册键与 DSH 当前源码的 tool.call.toolview 契约一致。尚未替换运行中的适配器包，不能据此声明已部署或浏览器验收通过。
- v0.13.0 最后三个组合页迁入模块注册，启动入口仅保留导航、宿主状态和页面挂载。前端 3 项测试、TypeScript 全项目类型检查、生产构建均通过。诊断页重复清理按钮移除，清理由备份页显式确认入口提供。浏览器布局和交互仍待验证。
- v0.12.0 Agent 页面和备份迁移页面独立注册，主应用删除相应页面分支及占位页。前端测试 3 项通过，包含有令牌但未确认时仍禁用清理按钮的渲染检查；生产构建通过。测试未执行实际备份删除或线程迁移，真实点击尚待验收。
- v0.11.0 五个内建前端页面转为模块入口自动发现；注册器启用归属和重复注册测试 2 项通过；Web 生产构建通过。组合页面和浏览器视觉验收仍未完成。此阶段未改变 Python 行为，不重复运行全量后端测试。
- v0.10.0 外部示例真实安装集成测试通过；提供同源卡片链接并验证危险地址拒绝。测试未安装到用户常用 Python 环境。
- v0.10.0 最终全量测试：319 通过；Web 生产构建通过。
- v0.9.0 生命周期顺序、部分启动失败回收、无效禁用配置、实际宿主禁用任务路由和清单：4 项通过。前端自动请求根据已启用插件清单调整，生产构建通过；浏览器行为尚待验证。
- v0.9.0 最终全量测试：313 通过；Web 0.9.0 生产构建通过。当前启用配置仅在重启生效，非热卸载。
- v0.8.0 全量 Python 测试：309 通过，覆盖迁移后的技能接口、Provider 操作、日志及流 WebSocket；新增三个插件独立进程导入检查。
- v0.7.0 两组插件的独立进程导入、未授权操作拒绝和宿主执行器分发：5 项通过。API 与注册表回归：63 项通过。
- v0.7.0 最终全量测试：306 通过；Web 生产构建通过。
- v0.6.1 Dashboard、API 与插件注册表测试：65 通过；包含新进程导入隔离、独立宿主四个端点和只读方法约束。
- v0.6.1 最终全量测试：301 通过；Web 生产构建通过。
- 最终全量 Python 测试：299 通过；Web 0.6.0 生产构建通过；DSH 源码工作树再次确认干净。
- 路由归属调整后全量 Python 测试：298 通过。
- 任务战场实现拆分后 API 与注册表测试：62 通过。
- 增加独立 FastAPI 宿主测试后注册表测试：7 通过，包括只读任务目录和未授权看门狗写操作拒绝。
- 默认 pytest 临时目录存在 Windows 权限错误，改用独立 basetemp 后运行成功；不可将最初清理阶段异常的运行算作通过。

原生状态必须以插件实现可独立导入和挂载为依据；仅重新分配路由或通过标签检查不足以证明完整解耦。
